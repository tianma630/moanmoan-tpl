<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>吐了个槽 - 技术设计完整方案</title>
    <style>
        * { box-sizing: border-box; }
        body {
            font-family: -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif;
            max-width: 1000px;
            margin: 30px auto;
            padding: 30px;
            background: #f8f6fc;
            color: #1e1e2a;
            line-height: 1.8;
        }
        .paper {
            background: white;
            padding: 40px 50px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.06);
            border-radius: 12px;
        }
        h1 { text-align: center; font-size: 32px; color: #2c1b4d; border-bottom: 4px solid #9b59b6; padding-bottom: 10px; margin-top: 0; }
        .subtitle { text-align: center; font-size: 18px; color: #7f8c8d; margin-bottom: 30px; }
        h2 { color: #2c3e50; border-left: 6px solid #9b59b6; padding-left: 15px; margin-top: 35px; background: #f4eef9; padding: 8px 15px; border-radius: 0 8px 8px 0; }
        h3 { color: #34495e; margin-top: 25px; font-weight: 600; }
        table { width: 100%; border-collapse: collapse; margin: 15px 0; font-size: 14px; }
        th { background: #2c1b4d; color: white; padding: 10px; text-align: left; }
        td { border: 1px solid #ddd; padding: 10px; background: white; }
        tr:nth-child(even) td { background: #fafafa; }
        pre {
            background: #1e1e2a; color: #e4e4e4; padding: 18px; border-radius: 10px;
            overflow-x: auto; font-size: 13px; font-family: 'JetBrains Mono', 'Courier New', monospace;
            border: 1px solid #333;
        }
        .highlight-box { background: #fef9e7; border-left: 6px solid #f1c40f; padding: 15px 20px; border-radius: 6px; margin: 15px 0; }
        ul, ol { padding-left: 25px; }
        li { margin: 8px 0; }
        hr { margin: 40px 0; border: 1px dashed #ddd; }
        .footer { text-align: center; color: #95a5a6; font-size: 14px; margin-top: 40px; border-top: 1px solid #eee; padding-top: 20px; }
        @media print {
            body { background: white; padding: 0; margin: 0; }
            .paper { box-shadow: none; padding: 20px 30px; border-radius: 0; }
            pre { background: #f4f4f4; color: #1e1e1e; border: 1px solid #ccc; }
            th { background: #555; }
        }
    </style>
</head>
<body>
<div class="paper">

<h1>💥 吐了个槽</h1>
<p class="subtitle">把不开心，吐给懂的人 · 完整技术设计文档 V2.0</p>

<!-- ================= 第一部分：产品概述 ================= -->
<h2>1. 产品概述与功能模块</h2>
<p><strong>产品定位</strong>：匿名情绪释放 + 共鸣社交平台。<br>
<strong>Slogan</strong>：把不开心，吐给懂的人。<br>
<strong>目标用户</strong>：职场压力族（95/00后）、大学生、生活琐事困扰者。</p>
<table>
    <tr><th>模块</th><th>功能</th><th>说明</th></tr>
    <tr><td>吐槽广场</td><td>信息流推荐</td><td>推荐/最新/附近/神帖，图文+文本</td></tr>
    <tr><td>匿名身份</td><td>动态马甲系统</td><td>随机生成昵称+头像，付费可固定</td></tr>
    <tr><td>情绪标签</td><td>分类筛选</td><td>#职场内耗 #恋爱脑 #原生家庭 等</td></tr>
    <tr><td>共鸣互动</td><td>拍一拍+回声</td><td>拍一拍代替点赞，回声仅限语音/表情</td></tr>
    <tr><td>树洞信箱</td><td>1v1匿名倾诉</td><td>随机匹配，24h自动销毁</td></tr>
    <tr><td>防网暴机制</td><td>敏感词+情绪检测</td><td>极端词汇触发心理援助</td></tr>
</table>

<h2>2. 交互原型设计（含关键流程与动效）</h2>
<h3>2.1 核心操作流程图</h3>
<pre>
启动App → 生成/读取马甲 → 进入首页广场
    ├─ 双击卡片 → 拍一拍（弹性缩放 + 手掌印粒子特效）
    ├─ 点击回声 → 弹出录音面板 → 长按录音（≤15s）→ 转文字审核 → 送达
    ├─ 点击“+” → 发帖编辑器 → 填内容/配图/选标签/设隐私 → 发布（能量+10）
    └─ 点击树洞 → 匹配动画（沙漏旋转）→ 1v1对话（显示24h倒计时进度条）
        └─ 倒计时归零 → 对话内容“沙化消散”永久删除
</pre>

<h3>2.2 关键交互状态描述</h3>
<ul>
    <li><strong>吐槽卡片</strong>：双击触发“拍一拍”（scale 0.95→1.0，橙色手印渐出）。长按弹出[收藏/不感兴趣/举报]。</li>
    <li><strong>回声录制</strong>：按住圆形按钮录音，松手即停。界面显示实时声波动画。违规内容提示“咱不骂人，换个方式吐”。</li>
    <li><strong>树洞匹配</strong>：匹配中显示动态沙漏；匹配成功展示对方马甲及匹配度（基于共同标签）。对话气泡分冷暖色。</li>
    <li><strong>空状态</strong>：无帖子时显示“这世界今天居然没人糟心？你来开第一吐！”并高亮发帖入口。</li>
</ul>

<h2>3. 数据库设计（MySQL DDL）</h2>
<p><strong>设计原则</strong>：匿名优先，不存手机号/邮箱，使用 <code>device_id</code> + <code>uuid</code> 标识身份。</p>

<h3>3.1 用户与马甲</h3>
<pre>
-- 用户表
CREATE TABLE `users` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `uuid` CHAR(36) NOT NULL DEFAULT (UUID()),
  `device_id` VARCHAR(64) NOT NULL,
  `current_masthead_id` INT UNSIGNED DEFAULT NULL,
  `energy` INT UNSIGNED NOT NULL DEFAULT 0,
  `total_posts` INT UNSIGNED NOT NULL DEFAULT 0,
  `total_pats_received` INT UNSIGNED NOT NULL DEFAULT 0,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_uuid` (`uuid`),
  KEY `idx_device` (`device_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 马甲表
CREATE TABLE `mastheads` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `nickname` VARCHAR(30) NOT NULL,
  `avatar_url` VARCHAR(255) NOT NULL,
  `is_fixed` TINYINT(1) NOT NULL DEFAULT 0,
  `used_count` INT UNSIGNED NOT NULL DEFAULT 1,
  `last_used_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user` (`user_id`),
  FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
</pre>

<h3>3.2 帖子与标签</h3>
<pre>
-- 吐槽主帖
CREATE TABLE `posts` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `masthead_id` INT UNSIGNED NOT NULL,
  `content` TEXT NOT NULL,
  `images` JSON DEFAULT NULL,
  `privacy_type` TINYINT NOT NULL DEFAULT 1 COMMENT '1-公开 2-树洞可见',
  `location_city` VARCHAR(20) DEFAULT NULL,
  `pat_count` INT UNSIGNED NOT NULL DEFAULT 0,
  `echo_count` INT UNSIGNED NOT NULL DEFAULT 0,
  `status` TINYINT NOT NULL DEFAULT 1,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user` (`user_id`),
  KEY `idx_privacy_time` (`privacy_type`, `created_at` DESC),
  FULLTEXT INDEX `idx_content` (`content`) WITH PARSER ngram
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 标签字典
CREATE TABLE `tags` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(20) NOT NULL UNIQUE,
  `icon` VARCHAR(50) DEFAULT NULL,
  PRIMARY KEY (`id`)
);

-- 帖子标签关联
CREATE TABLE `post_tags` (
  `post_id` BIGINT UNSIGNED NOT NULL,
  `tag_id` INT UNSIGNED NOT NULL,
  PRIMARY KEY (`post_id`, `tag_id`),
  FOREIGN KEY (`post_id`) REFERENCES `posts` (`id`) ON DELETE CASCADE,
  FOREIGN KEY (`tag_id`) REFERENCES `tags` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
</pre>

<h3>3.3 共鸣互动（拍一拍 & 回声）</h3>
<pre>
-- 拍一拍（防止重复刷）
CREATE TABLE `pats` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `post_id` BIGINT UNSIGNED NOT NULL,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_post` (`user_id`, `post_id`),
  FOREIGN KEY (`post_id`) REFERENCES `posts` (`id`) ON DELETE CASCADE,
  FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 回声（语音/表情包）
CREATE TABLE `echos` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `post_id` BIGINT UNSIGNED NOT NULL,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `masthead_id` INT UNSIGNED NOT NULL,
  `type` TINYINT NOT NULL DEFAULT 1 COMMENT '1-语音 2-表情',
  `resource_url` VARCHAR(255) NOT NULL,
  `duration` SMALLINT UNSIGNED DEFAULT 0,
  `text_preview` VARCHAR(100) DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_post` (`post_id`),
  FOREIGN KEY (`post_id`) REFERENCES `posts` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
</pre>

<h3>3.4 树洞信箱（1v1）</h3>
<pre>
-- 树洞会话
CREATE TABLE `treehole_sessions` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `initiator_id` BIGINT UNSIGNED NOT NULL,
  `receiver_id` BIGINT UNSIGNED NOT NULL,
  `matched_tags` JSON DEFAULT NULL,
  `expires_at` DATETIME NOT NULL,
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-活跃 2-销毁',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_initiator` (`initiator_id`),
  KEY `idx_receiver` (`receiver_id`),
  KEY `idx_expires` (`expires_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 树洞消息（物理删除依赖定时任务）
CREATE TABLE `treehole_messages` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `session_id` BIGINT UNSIGNED NOT NULL,
  `sender_id` BIGINT UNSIGNED NOT NULL,
  `msg_type` TINYINT NOT NULL DEFAULT 1 COMMENT '1-文字 2-语音 3-表情',
  `content` TEXT,
  `resource_url` VARCHAR(255) DEFAULT NULL,
  `duration` SMALLINT UNSIGNED DEFAULT 0,
  `is_read` TINYINT(1) NOT NULL DEFAULT 0,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_session` (`session_id`, `created_at`),
  FOREIGN KEY (`session_id`) REFERENCES `treehole_sessions` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
</pre>

<h3>3.5 安全与干预日志</h3>
<pre>
CREATE TABLE `safety_interventions` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `source_type` TINYINT NOT NULL COMMENT '1-发帖 2-树洞',
  `source_id` BIGINT UNSIGNED NOT NULL,
  `trigger_keyword` VARCHAR(50) NOT NULL,
  `action_taken` TINYINT NOT NULL DEFAULT 1 COMMENT '1-推送关怀卡片',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
</pre>

<h2>4. 视觉风格摘要</h2>
<table>
    <tr><th>维度</th><th>设计规范</th></tr>
    <tr><td>主色调</td><td>暗夜紫 (#2c1b4d) + 荧光黄 (#f1c40f)</td></tr>
    <tr><td>辅助色</td><td>低饱和灰蓝 (#7f8c8d)、脏粉色 (#e6b0aa)</td></tr>
    <tr><td>字体</td><td>圆润手写感（PingFang SC / 系统圆体）</td></tr>
    <tr><td>动效</td><td>弹性（spring）、抖动、沙化粒子消散</td></tr>
    <tr><td>全局</td><td>默认深色模式，保护夜间视力</td></tr>
</table>

<h2>5. 冷启动运营策略</h2>
<ul>
    <li><strong>种子用户</strong>：邀请100位“职业吐槽王”封闭内测，赠送限定马甲。</li>
    <li><strong>话题运营</strong>：每日推送“今日槽点”（如#周一综合症），激发UGC。</li>
    <li><strong>社交裂变</strong>：一键生成“吐槽海报”（含二维码），分享至朋友圈可围观。</li>
    <li><strong>校园大使</strong>：在10所高校招募“树洞管理员”，推广本地校园圈。</li>
</ul>

<hr>
<p style="text-align:center; font-size:20px; font-weight:bold; color:#2c1b4d;">💥🫂 把不开心，吐给懂的人。</p>
<div class="footer">—— 完整技术设计文档 · 2026年8月 ——</div>

</div>
</body>
</html>
